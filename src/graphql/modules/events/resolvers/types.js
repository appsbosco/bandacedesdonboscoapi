/**
 * events - Types
 * Generado por scaffold-graphql.js
 * (No sobreescribir: editá libremente)
 */

module.exports = {
  // TODO: resolvers por tipo
  // ejemplo:
  // Events: { field: (parent) => parent.field }
};
